spring:
  activemq:
    broker-url: vm://localhost?broker.persistent=false
    user: admin
    password: admin

eureka:
  client:
    enabled: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.11

logging:
  level:
    root: WARN
    com.epam: INFO